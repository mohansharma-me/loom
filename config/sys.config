[
    {loom, [
        {http, #{
            port => 8080,
            engine_id => <<"engine_0">>
        }}
    ]},
    {sasl, [
        {sasl_error_logger, {file, "log/sasl-error.log"}},
        {errlog_type, error}
    ]}
].
